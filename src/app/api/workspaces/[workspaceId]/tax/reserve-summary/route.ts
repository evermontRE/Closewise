import{NextResponse}from"next/server";import{getReserveSummary}from"@/data/tax";import{requireWorkspaceAccess}from"@/data/workspace-access";import{taxError,taxQuery}from"@/features/tax/http";type C={params:Promise<{workspaceId:string}>};export async function GET(r:Request,c:C){try{const{workspaceId}=await c.params,{supabase}=await requireWorkspaceAccess(workspaceId);return NextResponse.json(await getReserveSummary(supabase,workspaceId,taxQuery(r).year));}catch(e){return taxError(e,"Tax reserve summary");}}
