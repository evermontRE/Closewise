import{NextResponse}from"next/server";import{LegacyImportError}from"@/domain/legacy-import";import{LegacyWorkflowError}from"@/data/legacy-import";import{WorkspaceAccessError}from"@/data/workspace-access";export const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;export function importError(e:unknown){if(e instanceof LegacyImportError)return NextResponse.json({error:e.message,fields:e.fields},{status:400});if(e instanceof LegacyWorkflowError||e instanceof WorkspaceAccessError)return NextResponse.json({error:e.message},{status:e.status});if(e instanceof SyntaxError)return NextResponse.json({error:"The uploaded backup is not valid JSON."},{status:400});console.error("Legacy import API error",e instanceof Error?e.message:"Unknown error");return NextResponse.json({error:"Finance Studio could not complete the import request."},{status:500});}
