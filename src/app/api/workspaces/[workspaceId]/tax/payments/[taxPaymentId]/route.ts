import{NextResponse}from"next/server";import{getTaxPayment,mutateTaxRecord}from"@/data/tax";import{requireFinancialWriter,requireWorkspaceAccess}from"@/data/workspace-access";import{expectedVersion,mutationId,taxError,UUID}from"@/features/tax/http";import{parseTaxPaymentInput,parseTaxVoidInput}from"@/features/tax/input";type C={params:Promise<{workspaceId:string;taxPaymentId:string}>};
export async function GET(_r:Request,c:C){try{const{workspaceId,taxPaymentId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Tax payment not found"},{status:404});const{supabase}=await requireWorkspaceAccess(workspaceId);return NextResponse.json({taxPayment:await getTaxPayment(supabase,workspaceId,id)});}catch(e){return taxError(e,"Tax payment");}}export async function PATCH(r:Request,c:C){try{const{workspaceId,taxPaymentId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Tax payment not found"},{status:404});const{user,role}=await requireWorkspaceAccess(workspaceId);requireFinancialWriter(role);return NextResponse.json(await mutateTaxRecord({workspaceId,actorId:user.id,clientMutationId:mutationId(r),entity:"tax_payment",operation:"update",entityId:id,expectedVersion:expectedVersion(r),record:parseTaxPaymentInput(await r.json())}));}catch(e){return taxError(e,"Tax payment");}}export async function DELETE(r:Request,c:C){try{const{workspaceId,taxPaymentId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Tax payment not found"},{status:404});const{user,role}=await requireWorkspaceAccess(workspaceId);requireFinancialWriter(role);return NextResponse.json(await mutateTaxRecord({workspaceId,actorId:user.id,clientMutationId:mutationId(r),entity:"tax_payment",operation:"void",entityId:id,expectedVersion:expectedVersion(r),record:parseTaxVoidInput(await r.json())}));}catch(e){return taxError(e,"Tax payment");}}
