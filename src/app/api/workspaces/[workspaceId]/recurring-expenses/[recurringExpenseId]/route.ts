import{NextResponse}from"next/server";import{getRecurringExpense,mutateRecurringExpense}from"@/data/recurring";import{requireFinancialWriter,requireWorkspaceAccess}from"@/data/workspace-access";import{expectedVersion,mutationId,recurringError,UUID}from"@/features/recurring/http";import{parseRecurringExpenseInput,parseRecurringVoidInput}from"@/features/recurring/input";
type C={params:Promise<{workspaceId:string;recurringExpenseId:string}>};export async function GET(_r:Request,c:C){try{const{workspaceId,recurringExpenseId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Recurring expense not found"},{status:404});const{supabase}=await requireWorkspaceAccess(workspaceId);return NextResponse.json({recurringExpense:await getRecurringExpense(supabase,workspaceId,id)});}catch(e){return recurringError(e,"Recurring expense");}}
export async function PATCH(r:Request,c:C){try{const{workspaceId,recurringExpenseId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Recurring expense not found"},{status:404});const{user,role}=await requireWorkspaceAccess(workspaceId);requireFinancialWriter(role);return NextResponse.json(await mutateRecurringExpense({workspaceId,actorId:user.id,clientMutationId:mutationId(r),operation:"update",entityId:id,expectedVersion:expectedVersion(r),record:parseRecurringExpenseInput(await r.json())}));}catch(e){return recurringError(e,"Recurring expense");}}
export async function DELETE(r:Request,c:C){try{const{workspaceId,recurringExpenseId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Recurring expense not found"},{status:404});const{user,role}=await requireWorkspaceAccess(workspaceId);requireFinancialWriter(role);return NextResponse.json(await mutateRecurringExpense({workspaceId,actorId:user.id,clientMutationId:mutationId(r),operation:"void",entityId:id,expectedVersion:expectedVersion(r),record:parseRecurringVoidInput(await r.json())}));}catch(e){return recurringError(e,"Recurring expense");}}
