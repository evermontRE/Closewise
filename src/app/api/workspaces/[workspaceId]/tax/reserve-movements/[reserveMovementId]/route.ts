import{NextResponse}from"next/server";import{getReserveMovement,mutateTaxRecord}from"@/data/tax";import{requireFinancialWriter,requireWorkspaceAccess}from"@/data/workspace-access";import{expectedVersion,mutationId,taxError,UUID}from"@/features/tax/http";import{parseReserveMovementInput,parseTaxVoidInput}from"@/features/tax/input";type C={params:Promise<{workspaceId:string;reserveMovementId:string}>};
export async function GET(_r:Request,c:C){try{const{workspaceId,reserveMovementId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Reserve movement not found"},{status:404});const{supabase}=await requireWorkspaceAccess(workspaceId);return NextResponse.json({reserveMovement:await getReserveMovement(supabase,workspaceId,id)});}catch(e){return taxError(e,"Reserve movement");}}export async function PATCH(r:Request,c:C){try{const{workspaceId,reserveMovementId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Reserve movement not found"},{status:404});const{user,role}=await requireWorkspaceAccess(workspaceId);requireFinancialWriter(role);return NextResponse.json(await mutateTaxRecord({workspaceId,actorId:user.id,clientMutationId:mutationId(r),entity:"tax_reserve_movement",operation:"update",entityId:id,expectedVersion:expectedVersion(r),record:parseReserveMovementInput(await r.json())}));}catch(e){return taxError(e,"Reserve movement");}}export async function DELETE(r:Request,c:C){try{const{workspaceId,reserveMovementId:id}=await c.params;if(!UUID.test(id))return NextResponse.json({error:"Reserve movement not found"},{status:404});const{user,role}=await requireWorkspaceAccess(workspaceId);requireFinancialWriter(role);return NextResponse.json(await mutateTaxRecord({workspaceId,actorId:user.id,clientMutationId:mutationId(r),entity:"tax_reserve_movement",operation:"void",entityId:id,expectedVersion:expectedVersion(r),record:parseTaxVoidInput(await r.json())}));}catch(e){return taxError(e,"Reserve movement");}}
