import test from"node:test";import assert from"node:assert/strict";import{LegacyImportError,previewLegacyBackup}from"./legacy-import.ts";const backup={evermont:{schema:"evermont.finance.v1",schemaMajor:1,schemaRevision:11,producedBy:"Finance Studio",recordCounts:{ledger:2}},ledger:[{id:"L1",type:"expense",amount:"100.25",date:"2026-01-01"},{id:"L2",type:"income",amount:"500.00",date:"2026-01-02"}],clients:[{id:"C1",name:"Avery"}]};test("previews counts, duplicates, and control totals",()=>{const p=previewLegacyBackup(backup,new Set(["ledger:L1"]));assert.equal(p.counts.ledger.duplicates,1);assert.equal(p.counts.ledger.ready,1);assert.equal(p.controlTotals.sourceMoney,"600.25");assert.match(p.report.join(" "),/Nothing has been written/);});test("produces stable duplicate fingerprints",()=>{const a=previewLegacyBackup(backup),key="ledger:sha256:"+a.records.find(x=>x.legacyId==="L2")?.fingerprint,b=previewLegacyBackup(backup,new Set([key]));assert.equal(b.counts.ledger.duplicates,1);});test("rejects untrusted backup shapes",()=>assert.throws(()=>previewLegacyBackup({ledger:[]}),LegacyImportError));
